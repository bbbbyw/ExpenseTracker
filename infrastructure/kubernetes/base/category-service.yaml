apiVersion: v1
kind: Service
metadata:
  name: category-service
  namespace: expense-tracker
spec:
  selector:
    app: category-service
  ports:
    - name: http
      port: 3000
      targetPort: 3000
  type: ClusterIP

